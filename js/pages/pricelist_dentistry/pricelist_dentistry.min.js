import{pricelistDentistryList}from"./pricelist_dentistry_list.min.js";let pricelistBox=document.querySelector(".det"),pricelistCode="";function render(t){for(var[r,s]of Object.entries(t)){var i,c,l=`
            <thead>
                <tr>
                    <th id="keys">Код</th>
                    <th>Название услуги</th>
                    <th>Цена(в руб.)</th>
                </tr>
            </thead>
        `;let e=`
            <tr>
                <th class="sub-title"></th>
                <th scope="rowgroup" headers="keys" class="sub-title">${r}</th>
                <th class="sub-title"></th>
            </tr>
        `;for([i,c]of Object.entries(s))if(1==Object.keys(c).length)e+=`
                    <tr>
                        <td>${i}</td>
                        <th scope="row">${c[Object.keys(c)[0]].service}</th>
                        <td>${c[Object.keys(c)[0]].price}</td>
                    </tr>
                `;else{let t=!0;for(var[o,d]of Object.entries(c))t?(e+=`
                        <tr>
                            <td rowspan="${Object.keys(c).length}">${i}</td>
                            <th scope="row">${d.service}</th>
                            <td>${d.price}</td>
                        </tr>
                    `,t=!1):e+=`
                            <tr>
                                <th scope="row">${d.service}</th>
                                <td>${d.price}</td>
                            </tr>
                        `}l+=e,pricelistCode+=`
            <details>
                <summary>${r}</summary>
                <table class="pricelist-table">
                    ${l}
                </table>
            </details>
        `}pricelistBox.innerHTML=pricelistCode}render(pricelistDentistryList);